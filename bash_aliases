# Navigation
alias -- -="cd -"

# Unix
alias ll='ls -lahF'
alias ls='ls -FG'
alias ln='ln -v'
alias mkdir='mkdir -p'
alias tmux='tmux -2'

# Docker
alias d='docker'
alias dc='docker compose'
alias dce='docker compose exec'

alias dps='docker ps'
alias dcps='docker compose ps'

# Vim
alias vim-orig='vim'
alias vim='nvim'
alias vi='nvim'
