# Unix
alias ll='ls -lah'
alias ln='ln -v'
alias mkdir='mkdir -p'
alias pbcopy='xclip -selection clipboard'
alias pbpaste='xclip -selection clipboard -o'

# Git
alias g='git'

# Ruby
alias b='bundle'
alias z='zeus'

alias tmux="tmux -2"
